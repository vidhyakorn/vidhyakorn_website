# Hextra — https://github.com/imfing/hextra  (v0.12.3, vendored in themes/hextra)
# Layout modelled on https://developers.osuny.org/, which is itself built on Hextra.
# CUSTOMISING.md explains where to change colours, the homepage and the sidebar.

baseURL = 'https://vidhyakorn.github.io/vma/'
locale = 'en'
defaultContentLanguage = 'en'
title = 'Vidhyakorn Mahd-Adam'
theme = 'hextra'

enableRobotsTXT = true
enableInlineShortcodes = true

# Obsidian note templates live in content/_templates/ so they are visible in
# the vault, but they are not pages and must never be published.
ignoreFiles = ['content/_templates/.*', '\.obsidian/.*']

[outputs]
  home = ['html']
  page = ['html']
  section = ['html', 'rss']

[taxonomies]
  tag = 'tags'

[markup]
  [markup.highlight]
    noClasses = false
  [markup.goldmark.renderer]
    unsafe = true
  [markup.goldmark.extensions.passthrough]
    enable = true
    [markup.goldmark.extensions.passthrough.delimiters]
      block = [['\[', '\]'], ['$$', '$$']]
      inline = [['\(', '\)'], ['$', '$']]
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4

# ---------------------------------------------------------------------------
# Top navigation — left to right in weight order.
# ---------------------------------------------------------------------------
[[menu.main]]
  name = 'Research'
  pageRef = '/research'
  weight = 1
[[menu.main]]
  name = 'Writing'
  pageRef = '/blog'
  weight = 2
[[menu.main]]
  name = 'About'
  pageRef = '/about'
  weight = 3
[[menu.main]]
  name = 'Search'
  weight = 4
  [menu.main.params]
    type = 'search'
# Icons on the far right. ORCID and Google Scholar are added in data/icons.yaml.
[[menu.main]]
  name = 'ORCID'
  url = 'https://orcid.org/0009-0008-2468-8870'
  weight = 5
  [menu.main.params]
    icon = 'orcid'
[[menu.main]]
  name = 'Google Scholar'
  url = 'https://scholar.google.com/citations?hl=en&user=jgY_JOAAAAAJ'
  weight = 6
  [menu.main.params]
    icon = 'googlescholar'
[[menu.main]]
  name = 'LinkedIn'
  url = 'https://www.linkedin.com/in/vidhyakorn'
  weight = 7
  [menu.main.params]
    icon = 'linkedin'

# Extra links at the bottom of the Research sidebar.
[[menu.sidebar]]
  identifier = 'more'
  name = 'More'
  weight = 1
  [menu.sidebar.params]
    type = 'separator'
[[menu.sidebar]]
  name = 'Writing'
  pageRef = '/blog'
  weight = 2
[[menu.sidebar]]
  name = 'About & CV'
  pageRef = '/about'
  weight = 3

[params]
  description = 'Vidhyakorn Mahd-Adam — research assistant at MORU, Bangkok. Epidemiological and social modelling, health policy, and the history and philosophy of science and medicine.'
  externalLinkDecoration = true
  dateFormat = 'January 2, 2006'
  displayUpdatedDate = false

  # Publication metadata lookups — Crossref serves identified requests from a
  # faster pool. See layouts/_partials/paper-meta.html.
  crossrefMailto = 'vidhyakornadam@gmail.com'

  # ---------------------------------------------------------------------------
  # Homepage — the Osuny-style hero. Everything below the button (the card
  # grids of latest publications and writing) is generated automatically.
  # ---------------------------------------------------------------------------
  [params.home]
    badge = 'About Me'
    badgeLink = '/about'
    headline = 'τὸ τέλος τοῦ βαλάνου ἐστὶ τὸ γενέσθαι δρῦς'
    subtitle = ' “an acorn’s telos is to become an oak tree.” — Aristotle '
    intro = ' What do you think?  '
    buttonText = 'What do you think? Lets chat! '
    buttonLink = '/layouts/docs/single.html'
    # How many cards in each grid.
    publications = 3
    writing = 3

  [params.navbar]
    displayTitle = true
    displayLogo = false
    width = 'wide'

  [params.page]
    width = 'normal'

  [params.theme]
    default = 'system'
    displayToggle = true

  [params.footer]
    enable = true
    displayCopyright = true
    displayPoweredBy = true
    width = 'normal'

  [params.search]
    enable = true
    type = 'flexsearch'
    [params.search.flexsearch]
      index = 'content'
      tokenize = 'forward'
      # FlexSearch is kept in the repo (assets/lib/) instead of being fetched
      # from a CDN on every build, so a CDN outage can't break a deploy.
      js = 'lib/flexsearch.bundle.min.js'

  [params.editURL]
    enable = false

  [params.blog.list]
    displayTags = true
    sortBy = 'date'
    sortOrder = 'desc'
    pagerSize = 20

  [params.blog.article]
    displayPagination = true

  [params.toc]
    displayTags = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'

# ---------------------------------------------------------------------------
# Folder mounts. Declaring any mount replaces Hugo's defaults, so all of them
# are listed. The last one is the only addition: it makes static/img visible
# to Hugo's image processing as well, so card pictures kept in static/img are
# resized to small WebP files instead of being sent at full size.
# ---------------------------------------------------------------------------
[module]
  [[module.mounts]]
    source = 'content'
    target = 'content'
  [[module.mounts]]
    source = 'static'
    target = 'static'
  [[module.mounts]]
    source = 'layouts'
    target = 'layouts'
  [[module.mounts]]
    source = 'data'
    target = 'data'
  [[module.mounts]]
    source = 'assets'
    target = 'assets'
  [[module.mounts]]
    source = 'i18n'
    target = 'i18n'
  [[module.mounts]]
    source = 'archetypes'
    target = 'archetypes'
  [[module.mounts]]
    source = 'static/img'
    target = 'assets/img'
